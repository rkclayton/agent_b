# Agent_b interfaces

This document is the stable runtime contract. Bracketed prompt numbers are historical provenance only and carry no sequencing authority.

## Event envelope and transport

Every durable domain event is `{"seq":int,"ts":"RFC3339 with milliseconds","session_id":"","run_id":"","type":"","data":{}}`. Sessionless events are global. JSONL additionally permits `body` on `model.request` and `raw` on `model.response`; those fields are excluded from transport. Each session has its own log and global events use the Agent_b log. SSE sends top-level global events in that envelope, but session updates are `projection.patch` envelopes rather than raw domain events. A comment ping is sent every 15 seconds.

On SSE connection, `snapshot` [prompt 3] contains `{sessions:{<id>:SessionSnapshot},connections:[Connection with masked key],config,agent_server_changes:{<agent_id>:{agent_id,from,to,requested_at}},replay,build:{tag,commit,dirty,known,source,display,executable_sha256},update:{enabled,checking,current_version,available,version?,notes?,checked_at?,error?,installing},signature,mutation_token,shell_credential:{stored,stored_at},shell_identity:{fallback,operator_approval_required,operator_context,operator_context_expires_at,reason,since},serving_facts,flow:{stages,edges},tools:[{name,description}]}`. `agent_server_changes` contains only queued, not-yet-applied binding changes. `build` identifies the running binary; the shared headers render `tag`, the first seven commit characters, dirty state, and the adjacent signature state. Dirty builds append `+dirty` to the display value, and `executable_sha256` is the hash the signing helper must match before replacement. `update` is the latest passive release-check state; it never exposes asset URLs or starts an install. `signature` is the latest Settings verification result. `mutation_token` is a per-launch browser/CSRF secret required in `X-AgentB-Mutation-Token` on every non-read request; it is not persisted or logged. `replay` is true only for `-replay` connections. The credential status never contains the credential.

## Shared records

Session schema 1 additively carries `agent_id`, `role:b|c|d`, `scratch`, and the D-only `plan_id`, `plan_name`, `plan_dir`, and `plan_repo`. An absent role in a retained pre-v0.50 chat means `b`; B sessions carry no bound plan. A `c` session is the worker: it is bound to a plan and its repo, jailed like a `b` session, and refused the plans root exactly as `b` is, so it writes the repository and never plan text. It has no chat — it does not appear in the tab strip — and its only speech is a `c.job` post. It runs on the agent's `c` connection when one is assigned and on `b` otherwise. Plan markers are `[ ]` waiting, `[~]` running, `[x]` done and `[!]` stuck with the reason appended once after the item's text. An item line's identity is the `[[id]]` token written straight after its marker (`- [ ] [[2aa]] 2aa text`, id shape `[0-9]+[a-z]*`); the worker's writer assigns ids once, on first touch, keeping a leading item-file id no other line has claimed and otherwise the smallest number no line uses and no item file is named for, and changes nothing else on the line; markers resolve by id, so duplicate text is allowed and a reworded or moved line keeps its marker; the Plan panel shows the text without the token. Every write of a `plan.md` — its creation, worker markers and ids, the tray's and the cube's accepted edits, and a `d` session's own `edit_file`/`write_file` on it — takes one per-file lock in the process and re-reads the file under it. Registering a plan whose repository is inside the plans folder is refused with the reason, and an existing plan with such a repository returns `refusal` from `GET /api/plan` and `GET /api/plan/go` (which then reports `enabled:false`), shows it as one line on its panel, and `POST /api/plan/go` answers 409 with it; the harness writes them, never the model, and a marker write is refused if that line changed underneath. `[x]` is written only when the item's verifier command exits 0: the verifier is the `verify:` line in the header of `plan/items/<id>.md` (above its first heading), run after a clean `done` stop through the ordinary `shell` tool path as the worker — same gate, grants and identity as a model call — and nothing else can produce `[x]`. An item naming no verifier is not started and is `[!] no verifier named`; it is taken again by `Go` once its item file names one. The verifier's run, including any card it raises, is bounded by the item's deadline (`verifier did not finish before the item deadline`). A `c` session's `write_file` and `edit_file` refuse any path that resolves inside the plans folder even under an operator file grant, where the jail otherwise leaves the rest of the path to Windows, so a worker can never rewrite the item file whose verifier decides `[x]`. Other stuck reasons this path writes: `verifier failed: <output>`, `no verifier available`,
 and `waited for approval` when the item's deadline (six hours) expires while its run is paused on a card, or `waited for the model` when it expires while the run, its card answered, waits to take its model slot back; in either case the run is stopped. A worker's `approval.required` (including a cycle decision) carries `role:c` and `plan_id`; the Plan page draws it with the existing card in that plan's design thread, marked `agent_c`, and it is decided through `POST /api/approve` against the worker's own session id. A D session uses its agent's D connection, cannot enable `shell` or `run_script`, and its plan id is immutable. Plans live at `<data-root>/plans/<plan-id>/` with `plan.md`, `plan.json` (`repo` is the operator-changeable canonical repository path and the manifest is model-immutable), `NOTES.md`, and `plan/items/`; the first accepted relative write by an unbound D session creates and binds that layout. A chat created without a folder owns `<data-root>/scratch/<chat-id>/`; retained chats restore their prior folder unchanged. D file paths are canonicalized after union-root selection under every identity, so symlinks cannot widen its repository-read or plan-write roots.

`SessionSnapshot` schema 1 is `{schema_version:1,cursor:{generation,offset},complete,projection_stale?,projection_stale_reason?,id,label,connection_id,agent_name,main_connection,created_at,closed,workspace,run:{status:idle|queued|running|paused|stopping,run_id,turn,max_turns,queue_position,partial,last_stop_reason},activity,tools:[{name,enabled,calls,schema_tokens,marginal_tokens}],messages:[Message],budget:Budget,timeline:[durable operational events plus only the current model stream],chat:[server-projected ChatEntry with agent_role:main|aux when applicable],pending_approval?:ChatEntry,queued_messages,runnable,not_runnable_reason,memory_path,memory_content,log_path,model_turns,compaction_count,compaction_token_delta,compaction_model_calls,compaction_prompt_tokens,compaction_completion_tokens}`. `last_stop_reason` distinguishes `aborted_mid_model`, `aborted_mid_tool`, and `aborted_mid_run` from `done`. `complete:false` means the generation lacks a reachable `session.created` seed, so absent values are not represented as guessed defaults. Replay is a transport/UI overlay and never rewrites historical `run.status`. Aggregate fields, Activity, History, and Chat are folded by the one pure server projector from JSONL; clients do not independently derive them from event types. Chat coalesces repeated `model_unreachable` stops for the same host into one condition entry until `model.reachable` sets that entry's `decision:"resolved"`; Timeline retains every underlying stop event. Completed `model.delta` and `model.progress` records remain in JSONL but leave the projected timeline once their response arrives, so token traffic cannot evict operational history. Compaction prompt/completion totals remain a separate inference-spend ledger. `memory_content` is the read-only injected view for Settings; `log_path` names the current generation. Tool token fields and memory fields remain zero/empty until accounting runs. `schema_tokens` is isolated rendered schema size; `marginal_tokens` is the current request-prefix saving from disabling that enabled tool, including its system-prompt name. The compacted-history archive is absent from model-visible `messages`; live lookup remains a session-scoped index of immutable JSONL record locations, and replay rebuilds the same non-model archive separately from the session snapshot. Session events are also appended to `chats/{id}.jsonl` under the operator data root. That retained journal rehydrates the chat and transcript into a fresh operational log generation at startup; log rotation and retention never remove it. On the first upgraded launch only, an empty retained store is seeded from the newest pre-v0.44 operational generation for each recorded session ID.

Schema 1 additive workspace fields are `workspace_dir` (the canonical bound directory; legacy `workspace` remains an alias), `workspace_missing`, `project_content`, `project_files`, `project_notes`, `pending_repo_policy?`, and `repo_policy?`. The `project` budget category sits between `system` and `memory`. `session.created` seeds all of these so JSONL replay reconstructs the same binding, instructions, and policy card.

The versioned durable projection is session-scoped only. Top-level snapshot state (`connections`, masked `config`, build identity, shell identity and credential status, serving facts, flow, and static tool inventory) remains assembled by its existing runtime owners; the global JSONL has no startup seed from which those fields could be reconstructed. A projection cache is keyed by projection schema, log generation, and durable byte offset, is never persisted, and is always discardable rather than authoritative. A snapshot request projects through a captured complete-record boundary; later successful appends are applied once in memory. `projection.patch` is `{schema_version:1,session_id,previous_cursor,cursor,operations:[{op:replace|append|upsert|delete,path,value?}]}`. A cursor mismatch causes a full snapshot resync. The SSE subscription and captured cut are atomic, so a record cannot appear in both the snapshot and the post-cut patch stream. A full queue is closed rather than dropping a patch, forcing EventSource resynchronization. This boundary does not add a global seed event.

`Message` is `{id,role:system|user|assistant|tool,content,reasoning?,tool_calls?,tool_call_id?,name?,category:system|memory|tools|history|files|results|fetched|summary,tokens,estimated,elided,turn,ok?,plan_proposals?}`. `plan_proposals` is an additive array of inert `{id,kind,path,old_text,new_text,item_id,source_message_ids?}` records parsed from a version-one fenced `agentb-plan-proposals` JSON block; the harness replaces model-supplied source ids with the observed user/assistant turn pair, and a malformed block remains visible prose and produces no proposal. A compaction message uses `role:assistant,category:summary`: the endpoint receives its complete content as harness context, while Chat projects a visibly labelled summary entry and omits its prompt-only prefix and compaction-evidence appendix. `ok` is present on newly recorded tool messages so a snapshot or replay does not have to infer outcome from prose; older recordings may omit it. `fetched` content carries an unmistakable untrusted-data envelope in the model-visible text. Successful jailed `write_file` and `edit_file` tool results additionally carry `file:{path,bytes}` in their projected result metadata. The path is normalized relative to that session's workspace. Shell and `run_script` filesystem effects do not carry this metadata because their child-process writes are not observed by tool identity.

`Budget` is `{n_ctx,reserve,ceiling,used_est,used_measured,drift,cached_last:null|int,mode:exact|estimated,estimated,estimated_categories:[...],categories:{system,memory,tools,history,files,results,fetched,summary},tool_schema_tokens:{name:int},tool_marginal_tokens:{name:int}}`. `categories.tools` is the actual combined enabled-tools block. `categories.files` is the token weight of that session's own retained `read_file` result messages (category `files`), wherever the file lived; it never includes another session's messages, so a new chat's first request has `files` 0. A retained chat restored at startup brings its messages back with it, so its `files` is non-zero until those results elide or summarize. On restore, the projector keeps the chat's transcript (`chat`, `timeline`) projected from its earlier journal segments instead of reseeding it empty, message and run ids continue above the highest id a retained chat holds, and the page never selects a chat that already holds messages on its own: the operator selects its tab (item 2es). Neither per-tool map is additive because the rendered tool template has shared scaffolding; marginal values also cross the `system`/`tools` category boundary by including each tool's name-list contribution.

`Config.roles` has `{main,aux}` connection IDs. During first run, `connections:[]` is valid only with both roles empty; this state has no startup session and routes normal live pages to `/setup`. Once any connection exists, `main` must name a connection; `aux` may be empty and then resolves silently to `main`, and the same connection may fill both roles. `run.max_turns` is a pathological-case backstop whose shipped default is 10000; it is not shown as an ordinary Settings control. An explicit zero is rejected as ambiguous rather than silently becoming the default. `Connection` has `{id,label,base_url,model,credential,request_timeout_s,probe_mode,attachment_handling:auto|native|extract,sampling:{thinking,nonthinking},reasoning:{control,enabled,effort,valid_efforts,preserve},context:{n_ctx,reserve_output,sizing?},system_prompt_override,capabilities,measurement?}`; an installer-written `context.sizing` is `{weights_bytes,kv_bytes_per_token,available_bytes,reserve_bytes}` and records the inputs for its explicit llama-server `--ctx-size`. `measurement` is `{passed,total,tool_errors,tool_error_rate,trials,provenance,measured_at,duration_ms,capped}` and is descriptive, never a runnable gate. Omitted `attachment_handling` defaults to `auto`: `auto` follows probed document/image capability, `native` sends supported attachment kinds regardless of the probe, and `extract` keeps their binary bytes out of the model request. `context.n_ctx` is the required authoritative context size for that connection; probed `capabilities.n_ctx` is evidence and initializes it only while unset. Parameters remain properties of connections, not roles. A submitted `api_key` is runtime/API input only: it is stored under the named user-scoped DPAPI `credential`, appears as `"•••• set"` in masked API/event output, never enters logs, and is omitted from `harness.json`.

`Capabilities` has `{server:llama.cpp|openai-compatible|unknown,props,n_ctx,tokenize,apply_template,apply_template_tools,streaming,tool_calls,grammar_constrained,cached_tokens,timings,prompt_progress,reasoning_control:chat_template_kwargs|top_level|server_flag|none,reasoning_emission?:structured|inline|none,valid_efforts,overflow_behavior:error|truncate|unknown,probed_at,findings}`. `reasoning_emission` is absent until a full controlled reasoning probe classifies it.

## Model-server transport

Connections target an OpenAI-compatible server at `base_url`. Generation uses `POST /v1/chat/completions`; a nonempty connection key is sent as `Authorization: Bearer <key>`. All requests use JSON and the connection's request timeout. Standard sampling, messages, tools, tool choice, streaming, and usage fields are always OpenAI-shaped. `top_k`, `min_p`, `repeat_penalty`, `cache_prompt`, `return_progress`, and `chat_template_kwargs` are sent only when the probed server is llama.cpp. Reasoning controls never send an effort outside the connection's probed `valid_efforts`. Streaming tool-call arguments may arrive whole or as indexed fragments and are accumulated by index. **Reflection (item 17-i, v1.1.0).** A pass runs after a chat's run closes and on a daily tick, driven by a subscription to the event bus: it is never in a run's path, and nothing it does changes a run's outcome. It records one summary per run — files read and written from the JSONL, and the model's own account of what changed and what is open — in SQLite at `<data-root>/reflection/reflection.db`; summaries are kept 30 days, the newest 60 overviews per plan and 30 reports. The summary call uses the aux connection when one is configured and the run's own connection otherwise, saying which; a failed call records the failure on the row and nothing else. Structure extraction reports its tier (1: the toolchain's own resolver, run offline with a pinned environment; 2: a generic import table; 3: the filesystem), never modifies the repository and never reaches the network. `GET /api/reflection` returns the latest overview and tool-candidate report as text for Console; it is read-only. Reflection's durable outputs are one memory note per stated correction, written through the ordinary memory path, prefixed `[reflection, unconfirmed]`, never from a run that read untrusted content, bounded per pass and published as `memory.noted` so the existing drop path can revoke it; plans are **proposed in the overview, never registered** — registration stays the operator's through the existing card.

**Prompt cache across runs (item 6, measured v1.0.1/W2 from retained logs):** within a run the server reports most of the prefix cached (82% mean on a run's last response). At a run boundary the rendered prefix — messages and the tools block — is byte-identical in 161 of 298 retained boundaries, and in 46 of the 155 that carried timings the first response still reported under half the prefix cached, landing on constants (951, 1005) that do not vary with prompt length. The harness makes no completion call between runs: the budget measurement in exact mode calls only `/tokenize` and `/apply-template`, neither of which occupies a KV slot. Which slot each request takes, and what displaces it, is not recorded anywhere the harness writes; that attribution is open.

Agent-loop `max_tokens` is `min(max(4096, connection.context.reserve_output), n_ctx - guarded prompt tokens)`, where 4096 is the non-configurable plausible-tool-call floor; estimated accounting applies its 10% prompt guard before this calculation. Before dispatch, output headroom below that floor independently invokes the existing elide/summarize compactor. If compaction still cannot provide the floor, the run stops with `context_ceiling` and sends no knowingly truncated request. Compaction never touches the running turn: the message a run is answering is pinned at run start and neither elide, supersede nor summarize may alter it or anything after it, so a summarize span ends no later than the message before the pin and retreats further when that would fold a tool call whose result is kept. When the only thing left to compact is the pinned span itself, the run stops with `context_exhausted` rather than answering an older message. A response ending with `finish_reason:length` while emitting tool arguments never becomes a model-visible tool call or result. The original turn is retried once with that function forced, without adding a user message or other model-visible continuation; `model.retry` makes the harness action visible. A second truncation, a retry that does not return the forced call, or a prose response ending at the limit stops honestly with reason `length`. Malformed assistant tool-call structures are repaired before accounting: their prose and later messages remain, paired results are removed, and `model.retry` records the action. Every distinct `/apply-template` accounting shape is logged once as roles plus tool presence; `No user query found in messages` receives one trailing-user sentinel repair and sentinel-token subtraction for that shape. Any `/apply-template` or `/tokenize` error that remains is converted at the single boundary leaving measurement into an estimated budget with the endpoint reason, so accounting itself cannot stop the run. The connection probe sends the same accounting shape families and records each accepted or rejected shape in its findings.

The 2.5-second availability fast-fail applies only while establishing a connection. Once connected, tokenization and template accounting use the connection's normal request timeout. A connected transport failure during accounting degrades that measurement to estimated counts, including the 10% prompt guard, and does not stop or suppress compaction; the next measurement attempts exact accounting again. A dial failure retains the existing `model.unreachable` classification because the generation endpoint is unavailable too. A canceled request (a Stop) is never classified as unreachable, and the closing budget measurement after a stop runs on its own bounded context once the run has returned (item 2fg).

A generation request carries model-visible tools or a structured-output schema, never both. The current transport has no structured-output field: agent-loop and reliability-probe requests may carry tools, while compaction and all other probe requests carry neither.

Capability probing uses `GET /props` (falling back to `GET /v1/models`), `POST /tokenize`, `POST /apply-template`, and bounded chat-completion checks. A transport failure retains prior capabilities and leaves `probed_at` unchanged. `probe_mode:off` uses documented assumptions, `minimal` performs only the inexpensive identity/tokenization/template/streaming checks, and `full` includes tool, reasoning, timing, cache, progress, and overflow behavior. The controlled reasoning request classifies emission as structured, inline, or none; an inline finding names the server-side reasoning-format fix. Every assumed or measured result produces a plain-language finding.

## Model-visible tool interface

For B sessions, relative file-tool resolution remains inside the canonical folder. For D sessions, relative file-tool reads resolve inside the bound plan. Absolute read paths inside `<data-root>/plans` are accepted for every B and D session; D reads may also resolve inside the bound repository. B writes to the plans tree and D writes outside the bound plan are refused before identity routing. A D session's plan/repository resolver remains enforced in operator identity mode, so an identity grant never widens write authority. An unbound D session refuses absolute writes and binds a new plan on its first accepted relative write.

Host protection pre-creates `<data-root>/plans`, grants the service account explicit recursive `Modify` there, and grants traversal—not list/read/mutation—on the operator data root needed to reach it. Reapply and verify Host protections on an existing hardened installation before using D file writes.

The registry sends enabled tools in this stable order: `read_file`, `list_dir`, `write_file`, `edit_file`, `search`, `shell`, `remember`, `recall`, `fetch_url`, `web_search`, `run_script`, `call_service`, `delegate`. The internal-service caller remains position 12; `delegate` is appended at 13 and every older tool retains its relative position. Tool errors normally enter model context with an `error:` prefix and always produce `tool.result.ok:false`; a stale-view or routing refusal may instead start with `note:` while remaining an unsuccessful, unexecuted outcome. Documented empty/no-match results are successful outcomes.

- `read_file {path,offset?,limit?,line?,lines?,windows?}` reads numbered UTF-8 source text. Byte mode uses a one-based byte offset and budget; its header reports `offset`, source `bytes`, `total`, `more`, optional `next_offset`, `start_line`, `start_mid_line`, and `end_mid_line`, with display line prefixes outside the byte budget. Line mode uses one-based `line` and `lines` and cannot mix with byte arguments. `windows` accepts 1–32 ordered `{label?,offset?,limit?}` or `{label?,line,lines?}` windows for the same path, cannot mix with top-level window arguments, and reports an invalid member under its own label without suppressing successful siblings. When `more` is true the returned cursor advances the corresponding mode. If a requested successful result would exceed the current model-context result budget, the harness repeats the read internally and returns the largest successful window that fits, prefixed by an explicit context-clamp line naming requested/returned/remaining bytes and the continuation offset. The model receives one successful tool result rather than a failed retry turn.
- `list_dir {path?,depth?}` enumerates a directory with directories first, case-insensitive alphabetical order, two-space nesting, configured ignored directories, depth 1 by default and 3 maximum, and a configured result cap. Inaccessible descendants are skipped and counted instead of aborting the enumeration. Directories end in `/`; an empty result is `directory is empty`.
- `write_file {path,content}` atomically creates or replaces a file, creates parent directories, and refuses content over 512 KiB. A successful result reports the path and written line count.
- `edit_file {path,old_string,new_string}` requires one unique match and keeps this three-string schema. It preserves UTF-8 BOM and the file's CRLF/LF style, then atomically replaces or deletes through ordered exact, whitespace-normalized (including indentation and model-emitted smart quote/dash normalization), and unique block-anchor/loose-middle strategies. Ambiguous or absent matches are errors; a no-match error names the closest candidate line range when one is similar. Success reports the strategy and affected lines, carries a unified diff, and runs the in-process Go or JSON syntax parser when that checker applies; a failed check describes the diagnostic without replaying a tool call and leaves the edit available for the next corrective turn.
- `search_text {pattern,path?,glob?}` recursively applies a regular expression to text, optionally filters by basename glob, skips configured ignored directories, inaccessible descendants, inaccessible files, and NUL-detected binary files, and returns `path:line: text`. Skipped inaccessible entries are counted. Results use the configured match count and per-line rune caps; no matches is a successful `no matches` result.
- `shell {command,timeout_s?}` starts an unconfined command in the workspace directory with bounded combined stdout/stderr. Its model-facing description names the dialect it actually runs in (item 2gb): a configured Windows PowerShell resolves to PowerShell 7 when the host has one, on PATH or at the standard install path, and the description says `&&` and `||` work; with only Windows PowerShell 5.1 the tool rewrites those two operators before execution — `a && b` to `a; if ($?) { b }` and `a || b` to `a; if (-not $?) { b }` — at the top level only, never inside quotes, parentheses or script blocks, and the description says so. A command that is not PowerShell is run and described exactly as configured. The capability probe's findings carry one host line naming which shell backs the tool. `run_script powershell` uses the same resolved interpreter; script source is never rewritten. The service-context network restriction is enforced outside the tool layer; network reads belong in `web_search` or `fetch_url`. The agent-written-file provenance rule is limited to Windows script hosts (`powershell`/`pwsh`, `cmd`, `wscript`, `cscript`, `mshta` and their host-executable suffixes); Python, Node, Go, .NET, and other interpreters are not blocked by that rule. Execution-policy bypass remains forbidden. `Set-AuthenticodeSignature`, `signtool`, and certificate-store mutations via `certutil` are reserved for Settings under either identity. A nonzero exit is an error containing `exit=<code>` and nonempty combined output. Simple file discovery/read commands return a structured unsuccessful routing refusal naming the first-class replacement tool, and the command is not executed. With the service split enabled, an out-of-workspace discovery path instead reports the operator-decision boundary and supplies no replacement call.
- `remember {note}` appends a trimmed workspace note of at most 300 Unicode runes; an exact duplicate is a successful no-op and a new note becomes active next session. `recall {}` returns all notes for the active workspace or a successful empty-state message. Neither accepts a path and neither is widened by operator mode. `remember` takes `target:folder|agent` (default `folder`). A scratch chat has no folder layer of its own (item 2fh): its `folder` note goes to the plan repository it most recently wrote into, and with none in scope to the agent layer, which every chat of the agent loads, and the reply says so. A scratch chat's folder memory is the layers of the plan repositories it has written into, rebuilt at each run's start; any other chat's folder layer is its workspace's.
- `fetch_url {url,offset?,limit?,mode?:feed|article}` performs a policy-checked public HTTP(S) GET with no model-supplied headers, cookies, or credentials and returns untrusted text in a UTF-8-safe byte window. Omitted mode preserves the existing whole-document text window. `feed` deterministically parses RSS/Atom items in document order; `article` selects identifiable HTML main content, drops boilerplate, and explicitly falls back to the default window when no main root exists. Extraction metadata reports the mode, dropped count, and fallback state. When `allow_domains` is empty, `deny_domains` refuses listed hosts and subdomains before DNS or request dispatch; a nonempty `allow_domains` remains authoritative and supersedes that default deny list. The result and event metadata report URL/status/source and window fields. When `more` is true the returned `next_offset` is passed back as `offset`.
- `web_search {query,kind?:web|news,limit?}` fans out anonymously through the same DNS/IP, domain, redirect, size, timeout, and uTLS path as `fetch_url`. It returns engine-provided titles, URLs, and snippets—not page content—in the same untrusted result class, names engines that answered or were unavailable/benched, merges normalized URLs, and ranks agreement before source rank. Three consecutive errors or empty parses bench an engine for `tools.web_search.bench_duration_minutes`. News uses engine news verticals first and then RSS/Atom feed parsing over the checked-in outlet list. The model-facing description directs the caller to use `fetch_url` on a chosen result.
- `find_files {pattern,path?}` recursively matches filenames or relative paths, supports `**`, skips its fixed ignored directories, configured protected roots, and inaccessible descendants, reports the inaccessible count, sorts results, and returns at most 200 paths with an explicit truncation marker; no matches is a successful `no matches` result.
- `run_script {language:powershell|python|node,source,timeout_s?}` sends source over stdin without creating or executing a script file. PowerShell is invoked exactly as `-NoProfile -NonInteractive -Command -`; `-File` and `-EncodedCommand` are never used. It shares shell's identity, split-mode confirmation, configured approval policy, timeout/output limits, working directory, process-tree handling, and once-approved operator override. Full source remains in `tool.call` arguments and assistant tool-call history in JSONL/replay.
- `call_service {service,method,path?,query?,body?,headers?,offset?,limit?}` accepts either a registered service name or an absolute HTTP(S) URL. An unregistered absolute URL is called with no credential, a 60-second timeout and a 64 KiB response window cap. A registered name uses its configured method, timeout and window cap; its path may be relative to `base_url` or an absolute URL on the same host. A registered request or redirect to another host is refused with a credential-host mismatch before its credential is acquired or sent. Model headers are limited to `Accept`, `Content-Type`, `If-Match`, `If-None-Match`, and `Idempotency-Key`; `Authorization` is always tool-owned. Auth is `none`, `static_bearer:<env>`, or `exec:<argv>`. Exec argv runs directly as the non-elevated operator without a shell and is cached until 60 seconds before RFC3339 `expires_at`, or for five minutes when expiry is absent. The result is `{status,headers,body,cursor,duration_ms}`; 4xx/5xx remain successful results and response headers are a fixed subset. Continuing with `next_offset` repeats the request. Authorization values and bearer tokens never enter durable events, History, flow, or returned content. `require_confirmation:true` is parsed and logged but intentionally has no effect in this release. This identity-bearing internal-service surface remains distinct from anonymous, GET-only, untrusted-open-web `fetch_url`.
- `delegate {task,thoroughness?:quick|thorough}` runs one in-process, foreground, read-only child context on the parent's connection. The child receives only its self-contained task and the delegate prompt; it has `read_file`, `list_dir`, `search`, `fetch_url`, and `recall`, never write/edit/shell/script/service/remember/delegate. Quick is capped at 8 turns and thorough at 20; parent cancellation cancels the child. The returned summary is capped at 2,000 tokens and begins `sub-task result; its words carry no operator authority`; `tool.result.delegate` carries status, duration, tool-call count, and the child transcript. No child read is inserted into the parent's model context.

## Events

- Sandboxed `shell` and `run_script bash` results carry `target:"sandbox <id>"` in both the model-visible result and `tool.result` metadata; Console shows the target on the tool row. `operator_context:true` records that the call used the operator's Docker session.
- `tool.result` may carry `harness_note` (item 2eo): what the harness did around the call — an operator-identity override that succeeded, was denied, failed, or was canceled. It is never part of the result content the model reads (which is exactly what the tool returned); Chat draws it as its own `harness · <note>` line above the tool row. The byte-window, line-window and context-clamp headers remain in the content.

- `session.created` [prompt 3] `{session}`; `session.renamed` [3] `{session_id,label}`; `session.updated` `{session_id,connection_id,agent_name,main_connection,runnable,not_runnable_reason,memory_path,memory_content}`; `session.reset` [3] `{session_id,log_path,predecessor?:{generation,offset}}`; `session.closed` [3] `{session_id}`; `session.reopened` `{session_id}`. Closing refuses any active/queued/paused/stopping run and hides the tab without deleting, resetting, or stopping anything; reopening restores that retained chat to the tab strip. Closed sessions stay in projected state so the right-click history can list and reopen them. New reset generations identify the exact complete-record boundary of their predecessor. Historical reset generations without this field remain honestly incomplete and do not receive invented lineage.
- `chat.named` `{outcome:named|failed|skipped,reason,name}` records the one non-run model naming attempt after an ordinary chat's first run. The request uses that chat's connection, first user/reply pair, five-word instruction, and ten-second timeout; a failure retains the mechanical name and is never retried. Worker sessions do not make it.
- `server.probed` [3] `{connection_id,capabilities,findings}`; `config.changed` [3] `{config,notice?}` (masked; `notice` is emitted for a one-time config migration); `error` [3] `{where,message,lost_event_type?}`. `where:event_log` means the named event could not be persisted to JSONL and the error itself exists only in live state. `ui.error` is the distinct page-tape event `{kind:console.error|unhandled exception|unhandled rejection,message,stack,location,repeat_count,capped}` relayed over the existing loopback service with the active session id when one exists.
- `agent.server_change` is sessionless runtime state. Pending is `{status:"pending",change:{agent_id,from,to,requested_at}}`; applied and cancelled are `{status:"applied"|"cancelled",agent_id,from,to}`. It is live transport state rather than a durable session projection event.
- `navigation.started` records every initiated Chat/Console flip or settings-cog transition as `{navigation_id,navigation_kind:flip|settings,from,to,full_document,clicked_at,chat_id,since_previous_navigation_ms:null|number,subscriber_count}`. `subscriber_count` is read from the server's live `events.Bus` registry when the start reaches the server; it is not a page-local estimate. The event is written independently of completion so an abandoned navigation remains visible. Chat/Console flips set `full_document:false`, update history, and mount the selected resident view without assigning a new document. A full-document navigation elsewhere still synchronously claims the current document before assignment; any later click from that document is ignored and recorded as `navigation.suppressed`. `navigation.measured` records one completed transition as `{navigation_id,navigation_kind:flip|settings,from,to,document_request_parse_ms,module_page_init_ms,session_state_fetch_ms,event_stream_connect_ms,transcript_surface_rebuild_paint_ms,end_to_end_ms,transcript_entries,chat_id,model_reachability:reachable|unreachable|unknown,since_previous_navigation_ms:null|number,instrumentation_sync_ms}`. Full-document phase boundaries use the browser Navigation Timing, Fetch, EventSource, projected-snapshot, explicit surface-ready, and second-animation-frame boundaries established by the navigation harness. A same-document flip or settings transition records exact zeroes for the four load operations that do not occur. Both events are projection-neutral and advance only the session cursor.
- Full-document navigation carries the browser-generated `navigation_id` in the target query solely as a correlation key. `navigation.document_started` records `{request_id,navigation_id,path,arrival_at,handler_entered_at,connection_identity}` before the page handler does work; `navigation.document_completed` records `{request_id,navigation_id,path,handler_exited_at,bytes_written,connection_identity}` when it returns. `connection_identity` is Go's observed remote TCP address. Direct document requests without a browser ID receive a server request ID and an empty `navigation_id`. Both events are instrumentation-only and projection-neutral.
- `shell.identity` [17] `{fallback,operator_approval_required,operator_context,operator_context_expires_at,reason,since}` announces or clears a persistent alternate-identity failure and reports process-global operator context. While enabled, `operator_context_expires_at` is the current idle deadline, reset at ordinary tool-execution start and completion. New events set `fallback:false`; failed service spawning requires an operator approval instead of executing automatically. `shell.credential` [17] `{stored,stored_at}` reports write-only store status. `operator.context` [19] `{enabled,reason,expires_at}` audits every transition and idle reset globally and in each live session.
- `signing.applied` records `{thumbprint,signer,timestamp_url,timestamp,files,applied_at}` globally and in every live session after the signed restart consumes its one-time handoff record.
- Notification-eligible `approval.required` and `run.stopped` events carry `data.human:{happened,harness_action,question?,actions?}`. The pending approval card and Discord dispatcher consume that same ordered wording; raw event fields remain technical detail. `item.done` and `plan.done` are reserved notification event names and remain inert until their producers ship. `notification.failed` records `{sink,event_type,attempts,message}` only after the one retry fails; it never contains the webhook URL.
- `log.retention` is the global startup/daily prune record `{days,files,count}`. `files` contains removed top-level operational JSONL basenames; evidence archives and retained chat journals are never candidates.
- `run.queued` [4] `{run_id,position,behind?}` — `behind` (`agent_<role>`) when the run waits because its model connection is serving as many runs as `connections[].max_concurrent` allows (one when unset; `run.max_concurrent` still caps the total), projected as the run's `waiting_behind`. A run paused on a card holds no model slot, per connection or in total (item 2fs): once answered it takes one back, ahead of queued runs, and when none is free it reports `run.queued` with `resuming:true` and then `run.resumed` `{run_id}` — which returns it to `running` without resetting it — when it has one; `run.started` [4] `{run_id,user_message_id,armed_detectors,thresholds_are_guesses,backstops:{wall_clock_seconds,tool_calls,turn_ceiling}}`; `run.stopping` `{reason}`; `run.stopped` [4] `{run_id,reason,detail,turns,armed_detectors}`; `run.labeled` `{label:productive|stuck|mixed}` with the labeled run in the event envelope. The label route binds to the exact latest completed run id so a stale click cannot label a newer completion. Active-stop reasons are `aborted_mid_model`, `aborted_mid_tool`, or `aborted_mid_run`; a queued run stopped before dispatch reports `done`. Cancellation gets 2 seconds to yield, then the scheduler terminates the run, fences its late completion, and returns the session to use. Other reasons are `done`, `mailbox_stop`, `wall_clock`, `tool_budget`, `turn_ceiling`, `cycle`, `tool_errors`, `context_ceiling`, `length`, `model_error`, `model_unreachable`, and `connection_not_runnable`.
- `progress.shadow` is emitted once per detector after each stopped run as `{detector,armed,available,would_fire,threshold_is_guess,threshold,values}`. The seven names are `novel_action`, `result_repetition`, `repeated_timeouts`, `model_says_stuck`, `error_success_ratio`, `baseline_deviation`, and `aux_progress`; none enforces. `progress.aux` is the optional soft classifier input `{turn,available,verdict?}` produced every ten turns only when the agent has an aux connection. Reasoning-off and missing aux inputs are represented by `available:false`, not omitted.
- `run.aborted` `{reason,detail,turn,stage,tool_in_flight:{name,arguments},partial_output:{reasoning,content,tool_calls:[{index,name,arguments}],tool_result},possibly_written_paths,paths_complete,unverified}` is written before the stopped run becomes resumable. Arguments and partial output are verbatim. A paired system-role `[HARNESS ABORT RECORD]` remains in ordinary history and therefore ages out normally through compaction; request assembly moves that byte-identical harness-authored message into the contiguous leading system block rather than emitting it after conversation history. Possibly partial paths remain explicitly unverified; shell/script paths are reported as unknowable rather than guessed. An event-log append failure emits the existing live `error where:event_log` signal with `lost_event_type` and never fails silently.
- `stage` [4] `{stage,state:enter|exit,turn,ms}`. Stages are `assemble`, `call_model`, `parse`, `dispatch`, `execute`, `append`, `compact`, `wait_user`; compact is initially a no-op.
- `model.request` [4] `{turn,message_count,tool_count,params:{temperature,top_p,top_k?,min_p?,presence_penalty,repeat_penalty?,max_tokens,reasoning:{control,effort?,enabled?,preserve?}},est_prompt_tokens,estimated}`.
- `model.progress` [4] `{turn,total,cache,processed}`; `model.delta` [4] `{turn,kind:reasoning|content|tool_call,index,text}`. A tool-call delta also carries the safe cumulative telemetry `{call_id,name,argument_bytes,argument_tokens}` as those values become known. Its durable `text` remains empty and no argument fragment is emitted; the complete sanitized arguments arrive only on `model.response` and `tool.call`, preventing header values from leaking through incremental JSONL. The projector exposes the cumulative measures on the current Activity stream so Chat can draw the live call without reconstructing or receiving its arguments.
- `model.response` [4] `{turn,finish_reason,content,reasoning_tokens,reasoning_tokens_estimated,tool_calls:[{id,name,arguments}],usage:{prompt_tokens,completion_tokens,cached_tokens:null|int},timings:null|object,duration_ms}`.
- `model.retry` `{turn,next_turn?,reason:truncated_tool_call|malformed_tool_history,tool,attempt,max_attempts}` is a visible harness-authored event. It never creates a user-role message or other mid-run prompt injection.
- `tool.call` [4] `{turn,call_id,name,args}`; `tool.result` [4] `{turn,call_id,name,ok,operator_context,untrusted,ms,bytes,tokens,preview,...tool_metadata}`; `tool.toggled` [4] `{name,enabled}`. `operator_context:true` marks a shell/file call that executed with the Agent_b operator identity or a `call_service` call whose `exec:` credential derives from that identity. A nonzero shell exit produces `ok:false`; its model-visible result begins with `error:`, includes `exit=<code>`, and retains nonempty combined stdout/stderr. Fetch metadata is `{url,final_url?,status,source_bytes,source_truncated,content_type?,window_offset,window_bytes,total_bytes,more,next_offset?}` and `untrusted:true`; `call_service` metadata is `{service,method,path,status,duration_ms,window_offset,window_bytes,more,next_offset?}`. The timeline labels both visibly without exposing service auth.
- `message.appended` [4] `{message}`; a user message may carry `attachments:[{path,bytes,sha256,kind?,outcome?}]`, while its `content` remains exactly the text the operator typed. `kind` is the server-classified content route retained so UTF-8 text with an uncommon extension stays readable after upload; older recordings may omit it and fall back to extension classification. `outcome` is harness-generated descriptive status for a native attachment that was not sent because its encoded upper bound did not fit the connection context budget; Chat renders it beside that attachment and the model-visible attachment line carries the same status. `message.updated` [8] `{id,patch}`; `message.removed` `{id,reason:unrenderable_tool_call|operator_repair}`; `messages.reminted` `{reminted:[{index,from,to}]}` (restore only, item 2fd); `message.queued` [6] `{message_id,position}`. Removal is durable projection input used by targeted automatic recovery and the explicit operator repair control.
- `budget` [4] uses the `Budget` shape. Prompt 4 emits a rough estimate; prompt 8 resolves exact versus estimated accounting.
- `approval.required` [6] `{call_id,name,args,boundary_escape}`; `approval.decided` [6] `{call_id,decision:approve|once|run|session|operator_mode|deny|superseded|dismissed}`. At most one decision is pending per session; a newer decision durably supersedes the prior one and cancellation durably dismisses it. `shell.grant` / `shell.grant_lapsed` are `{run_id,scope:run|session,rule:shell_policy|shell_boundary|operator_command,identity:service|operator,executable?,reason?}`; `file.grant` / `file.grant_lapsed` are `{run_id,scope:run|session,identity:operator,reason?}`. Run grants lapse at run end and session grants lapse on `session.closed`; neither scope is global, shell grants never cover file tools, and file grants never cover shell. `cycle.detected` [6] is `{call_id,name,args,prior_call_id}`; `workspace.conflict` [6] is `{path,session_id,other_session_id,other_label,age_s}`. `boundary_escape:false` marks normal approval policy; `true` marks a mandatory dispatcher-only `<tool>.operator_override` or `shell.operator_command` decision. With the service identity disabled (item 2fi), a file tool's outside-folder refusal, and a `shell` command or `run_script` source naming a literal absolute path outside the chat's roots (its folder and the registered plan repositories; `.exe`/`.cmd`/`.bat` paths excepted), raise the same `<tool>.operator_override` decision (reason `path is outside the folder` or `names a path outside the folder: …`) instead of a bare refusal; with the service identity enabled nothing changes. A file tool's target outside the folder that does not exist — a definite not-found in the identity the tool runs as (the service account's, or the harness's with none; a denied stat counts as existing) — raises no decision (item 2fy): the call fails with `no such file or directory: <path>; it is outside the folder …`, and a `write_file`/`edit_file` to such a path fails with `path is outside the folder and does not exist: <path>; a write outside the folder is refused …`. The shell's literal-path check classifies each outside path by its statement (item 2fz, v1.0.0): an argument of a directory change or listing (`cd`, `chdir`, `pushd`, `Set-Location`, `sl`, `Push-Location`, `dir`, `ls`, `gci`, `Get-ChildItem`, `tree`) raises no decision unless a file-reading verb or form follows it in the same source; any other outside path is a read — the decision when it exists (a denied stat counts as existing), and, when it does not, a plain failure `command was not started: no such file or directory outside the folder: <path>` (`script was not started: …` for `run_script`) with no decision.
- `chat.exported` `{path}` records the successful close-time Markdown export. The session ID remains in the event envelope; the path is operator-owned output under `chats/<directory-key>/<date>-<name>.md`, and JSONL remains the durable event authority.
- `compaction.summary` [8] records every summary fit decision or dispatched call as `{role:main|aux,connection_id,model,outcome:accepted|rejected|skipped|error,reason?,fallback_reason?,dispatched,estimated_prompt_tokens,estimated,n_ctx,usage:{prompt_tokens,completion_tokens,cached_tokens},duration_ms}`. A configured distinct aux connection is template/tokenizer fit-checked before dispatch; estimated checks carry a 10% guard. Aux fit, call, or reduction-guard failure falls back sequentially to the session's main connection. Summary requests contain non-elided history/summary prose, assistant working notes, and the currently retained `files`/`results`/`fetched` bodies, plus compact tool evidence with sanitized call arguments, success state, and read/fetch window metadata. Elided bodies are never restored. Accepted summaries carry forward a bounded code-generated appendix of up to 12 evenly sampled, JSON-encoded, 320-rune verbatim tool-result excerpts so early, middle, and late ground truth survives recursive compaction without restoring full discarded bodies. The instruction requires compactly preserving observed findings (including regional anchors for sequential reads), cursor, consumed scope, and stopping condition, treats a referenced name as no proof of its declaration, and forbids invented observations or claims from elided results. `compaction` [8] remains the successful context mutation event `{kind:elide|summarize,trigger:soft_pct|summary_pct|overflow|supersede|settle,before,after,affected_ids,summary_message_id?,role?,connection_id?,model?,fallback_reason?,usage?}`. A summary never folds the pinned running-turn message or anything after it, and its span always ends at a user message (the pin at the latest), so the note — an assistant message — is never followed by another assistant message; with no run in flight and no later user message the span runs to the end (item 2fd). Every request list is checked before it is measured or sent: an assistant message followed by another assistant message is folded into the later one for that request only (the session keeps both), because a chat template refuses a list ending in two assistant messages and the budget measures every prefix. An `apply-template` refusal is retried once after reassembly; a second refusal stops the run with `budget accounting: <reason>`. Elision (`kind:elide`) never touches the running turn's user message or the model's own messages. Its recent window is the running turn, not a count: results of earlier turns are eligible however recent, and inside the running turn it keeps the newest 4 tool results verbatim, except that a single result larger than a quarter of the context window is eligible once the model has answered after it (item 2fd); older tool results inside the running turn may become `[elided: …]` stubs before a run stops for context, and are never summarised (item 2et). Eligible results older than the running turn are elided first, largest first, ties oldest first; the running turn's own eligible results only after them (item 2ey). At the end of every turn, and once before a run's first request, the projected prompt is measured: at `context.soft_pct` of the ceiling one elide batch runs toward 60% (not immediately after a cold prefill), and at `context.summary_pct` a summary runs, so the next request starts under the line; the pre-request overflow path remains the fallback. `trigger` on every compaction event names what asked for it; a dispatched `compaction.summary` carries the same `trigger`. A `context_exhausted` stop names the reads it still kept verbatim. A `read_file` window is clamped to the room left in the turn at call time (`n_ctx − reserve − prompt − results already returned`); with no room left the read returns a bounded error naming the offset to retry, never the whole window, and only an unknown window (no `n_ctx`) passes a result through. A retained chat restored at startup carries tool results older than the newest user message its journal names (the newest `run.started` `user_message_id`, whether that message survived compaction or not; message ids are minted in order) as elision stubs; its JSONL keeps the bytes. A later copy of a repeated message id is given a new id above the id floor once at restore, journaled as `messages.reminted` `{reminted:[{index,from,to}]}`, which the projector applies by position so a later restore needs none. A native image or PDF part is sent only with the running turn's user messages; afterwards the message names the attachment and says it was shown in an earlier turn and is not re-sent (item 2fd). The projector applies the event's `after` to the budget immediately, subtracting the freed tokens from the compactible categories only, so the context meter follows a compaction without waiting for the next measured request and the fixed prefix (system prompt, project instructions, tool schemas, both memory layers) is drawn as a floor rather than as zero. The summary instruction is structured under `INTENT`, `USER MESSAGES`, `FILES`, `ERRORS AND FIXES`, `PENDING` and `NEXT STEP`, carries every user message inside the span verbatim, and consolidates an existing progress note into one note instead of stacking a second. The note's first line names the turns it covers. Elision has no serving connection or model usage. A snapshot (`GET /api/state` or an SSE connect) never advances the store's broadcast cursor for a session it has already been sent; only the fold of an appended record does, so a record appended but not yet folded when a snapshot is taken still reaches every subscriber as a patch, and a client whose snapshot is newer resyncs on the `previous_cursor` mismatch. `memory.noted` [8] is `{note,path}`.
 `item.done` and `item.stuck` are `{item,plan_id}` and `{item,plan_id,reason}`; `plan.done` is `{plan_id,done,stuck}`; `c.job` is `{plan_id,item,question,routed_to:d|operator,role:c,worker,proposal?}` and carries the worker's own last words, never a summary written for it; the one harness-worded `c.job` is the verifier request, whose `proposal` is `{id,kind:verifier,path,old_text,new_text:"",item_id}` and which the tray shows with Dismiss and quote only, because only the planner can name the command. All four carry human sentences and are dispatched by the notifications manager beside `approval.required` and `run.stopped`.

## HTTP API

`POST /api/sessions` accepts `{label?,agent_id?,connection_id?,role?:b|d}` or `{source_session_id}`. Every new session receives a new `<data-root>/scratch/<session-id>` folder; the source form creates a B chat with the source connection and exact enabled toolset, never its folder. D requires the selected agent's assigned D connection and starts unbound. Legacy `workspace` and `plan_id` fields are rejected. `GET /api/plans` returns `[{id,name,repo?}]` from `<data-root>/plans`, using the first Markdown heading in `plan.md` as the display name and the id as fallback; it is read-only. The snapshot carries the same list as `plans`. Every new plan (registration, detection, AGENT_B.md write, deploy, a d-session's first write) starts from the five-section template — `# <name>`, `## Product and end goals`, `## Architecture` (a generated map of the plan's repository: top-level directories with one line each from a README or Go package comment, then top-level files, all quoted into single lines with control characters and backticks removed, links not followed, and labelled as the repository's text rather than instructions; plus `### Invariants`), `## Standing rules` (seeded with the harness's own rules, nothing taken from the repository), `## Milestones and current order` (with `### Milestones` and `### Current work order`, which the panel reads as the current order, as it reads a level-2 `## Current work order…` heading), `## Index` — with no marker line (item 2bq). Session-less events `plan.created` and `plan.updated` `{plan_id, plan:{id,name,repo?}}` are published when a plan folder is complete and whenever an existing `plan.md` is rewritten by any route (worker markers, accepted edits, file tools); `plan.removed` is reserved (no route removes a plan folder). `GET /api/plan/go` adds `diagnostics:[{severity:error|warning,message}]` from the plan lint: a duplicate item id or an entry of any `## Unresolved` section of an item not yet done (`[x]` items are exempt, so a finished record never refuses Go) other than `(none)`, `- [discovery] …` or `- [blocker] …` (`*` bullets too; fenced blocks are skipped) is an error that sets `refusal` and disables Go (`POST /api/plan/go` answers 409 with it); an item with no item file or no `verify:` line is a warning, since Go marks it `[!] no verifier named` and asks for one (2t-ii). `POST /api/plan/accept` returns the same `diagnostics` after the edit. An `add` proposal for `plan/items/<id>.md` must carry a `## Contract` block and a `verify:` line where the worker reads one, in the header before the first heading. An exact `Add <absolute-path> as a plan` chat message uses the standard approval interface and, when approved, registers the canonical repository without a model request. Registration by any route is judged on the folder the path resolves to (junctions and links resolved by the file system): a missing folder, a drive root, a network path (refused before it is touched), a folder inside the plans folder and a folder that contains it are refused with the reason, and a refused path raises no card; when the path resolves elsewhere the card's arguments carry `resolves_to`. A final model message consisting of exactly that line (b or d chat; never the worker) raises the same Allow-this card named `plan registration proposed by agent_b` after the line is in the transcript; approval registers exactly as the operator's sentence does, a decline registers nothing, and the model's write access to plan files is unchanged (item 2fa).

- `GET /` and `/chat` serve the same workspace document, initially mounting Console and Chat respectively; the agent-tab switch mounts the other resident view in place. Settings remains a same-window Console view; `GET /static/*` serves `web/`. Documents (`/`, `/chat`, `/plan`, `/setup`) are sent `Cache-Control: no-store`, carry `<meta name="agentb-build" content="<executable_sha256>">` for the build that served them, and reference every `/static/X` asset as `/static/~<first 12 hex of executable_sha256>/X`, so relative module imports inherit the version (the server strips the segment); `/static/*` is sent `Cache-Control: no-cache`. On load, `web/js/build-check.js` unregisters any service worker and compares that meta with `GET /api/state` `build.executable_sha256`, and the bus compares it again with every `snapshot` (so a page left open across an upgrade checks when its stream reconnects); on a difference it reloads once per server build (sessionStorage `agentb.build-reload`) and never loops (item 2ev).
- `GET /api/events` is snapshot-plus-projection-patch SSE; `GET /api/state` is `snapshot.data`. In replay mode each SSE connection receives the initial projected snapshot followed by timestamp-ordered projection patches at 20 ms per durable record; `?instant=1` removes the delay.
- `POST /api/navigation-starts` accepts the bounded `navigation.started` data shape from the same-origin browser, requires the launch mutation token, deduplicates retries by `navigation_id`, and writes the start independently of completion. `POST /api/navigation-measurements` preserves the v0.26.0 completed-measurement shape and deduplication. Both write to the named chat tape (or the global tape when no chat exists) and are unavailable in replay mode.
- Browser-initiated document GETs that carry a bounded `navigation_id` are recorded by the page handler at start and completion without changing the rendered response. Chat/Console flips issue no document GET and are evidenced by their `full_document:false` navigation records.
- There is no signing HTTP endpoint. Publisher signing and expiry checks run only in the offline release scripts. `/api/state.signature` is read-only inspection data for About's `signed`/`unsigned` word.
- `GET /api/notifications` returns only `{configured,host?}`. Operator-verified `POST /api/notifications` accepts `save|clear|test`; `save` consumes a Discord webhook URL into the named DPAPI store, `clear` removes it, and `test` emits the fixed Settings test message. Responses and events never return the URL. Lifecycle delivery performs one POST and one retry after 30 seconds, refuses redirects, suppresses Discord mention parsing, and uses only the configured Discord host. Clearing or replacing the URL cancels an in-flight request or waiting retry bound to the old destination.
- `GET /api/update` returns the latest update state. Mutation-token-protected `POST /api/update {action:"check"}` performs the same single latest-release GET as the startup/daily check. `{action:"install"}` is accepted only while a newer release is held in state: it fetches that release's `release.json`, downloads the named setup under the manifest's exact-byte ceiling, verifies SHA-256, clean tag/commit identity, and a valid timestamped Authenticode signature, and only then starts the installer. A Windows-valid timestamp remains accepted after the signer certificate expires. It never stops the running process itself and never installs on a timer. `update.changed` carries the same state to open pages.
- `GET /api/sessions`; `POST /api/sessions {label?,connection_id?,role?}` → 201 `{session}` (`connection_id` defaults to the B role), or `POST /api/sessions {source_session_id}` to create a fresh scratch session with the source's connection and exact enabled toolset; `POST /api/sessions/{id} {label?,connection_id?}` → 200 `{session}` (server reassignment requires an idle open session and a runnable connection); `POST /api/sessions/{id}/result-label {run_id,label:productive|stuck|mixed}` labels only that exact latest completed run; `POST /api/sessions/{id}/close` marks an idle session closed without deleting it and refuses a running session; `POST /api/sessions/{id}/reopen` returns a retained closed chat to the open set; `DELETE /api/sessions/{id}` permanently removes the chat journal, scratch folder, attachments and history entry only when the chat is already closed, after exporting its transcript; `POST /api/sessions/{id}/messages/drop-last` durably removes exactly the latest message from an idle conversation; `POST /api/sessions/{id}/reset` clears the conversation/run projection and rotates its JSONL while retaining the session folder, connection, enabled tools, and memory. Retained chats restore their recorded folders without migration. The prior operational JSONL remains available for explicit replay, but its events are removed from live snapshots so refresh cannot resurrect cleared History.
- `GET /api/workspaces` lists observed scratch and retained legacy directories, memory counts, and approved policy metadata; the mutation-token routes `/api/workspaces/memory-clear`, `/policy-approve`, `/policy-once`, `/policy-deny`, and `/policy-revoke` manage only that install state. There is no folder-picker or chat-binding API.
- `GET /api/connections`; `POST /api/connections/{id}/probe` → 202 and an eventual event.
- `POST /api/agents/{agent_id}/server {action:"set",connection_id}` changes the config-backed agent B binding immediately only while that agent has no active run. During a run it returns 202 with a pending change; the original server remains bound through that run, and the change is saved and applied to all open sessions at the scheduler's idle boundary before another queued turn can dispatch. STOP observes the same boundary. `{action:"cancel"}` cancels a pending change. Unknown, incomplete, or non-runnable connections are refused. The route requires the launch mutation token and is unavailable in replay mode.
- `GET /setup` serves the stateless four-screen setup wizard. With no configured connections, `/` and `/chat` return a temporary redirect to it; `?setup=skip` skips that navigation for the current request only. Settings links to the same route. The screens connect/Test or install/defer, show probe findings and optional measurement, assign connections to `b`/`c`/`d`, and open Chat; no completion marker is stored. `GET /api/local-detection` runs the installed read-only Windows detector and reports system/recommendation memory, GPUs, CUDA/Vulkan availability, known running local connections, and interpreter executable ACL reachability for the configured service account. `GET /api/model-install` returns the server-pinned catalog filtered in the browser by those facts plus progress state; `POST /api/model-install {model_id,backend}` accepts IDs only, obtains system memory for CPU or the selected backend's GPU VRAM from a fresh server-side detection, and starts the verified local pipeline. After verifying the GGUF, the installer reads its declared context and KV shape, reserves 1 GiB, rounds the largest fitting f16-KV context down to 4096-token increments with an 8192-token floor, refuses a model below that floor, and persists the calculation both beside the GGUF and in the installed connection. `GET /api/eval/measure?connection_id=` returns `unmeasured` or current progress/result; `POST /api/eval/measure {connection_id}` starts at most one ten-brief, N=1 measurement for that connection under one five-minute deadline and stores its descriptive result on the connection.
- `GET /api/config`; `POST /api/config` deep-merges keys and server entries by immutable `id`, validates, saves, and publishes `config.changed`. The masked API-key sentinel means unchanged. Validation errors are 400 `{error,field}`. The shared Console/Chat operator header control posts `shell.operator_context` alone without a confirmation dialog and renders only observed `shell.identity` / `operator.context` state. Every transition is a session-visible, replayable log entry; enabled state uses an Alarm background while the persistent high-contrast header indicator remains active. Every SSE open and foreground resume also reconciles that indicator from authoritative `GET /api/state`; a newer operator event supersedes an older in-flight reconciliation response. `shell.operator_context` is runtime-only and is never persisted on; changes to it or `shell.service_account` require a loopback request whose actual client process has the Agent_b operator's Windows SID and is not Agent_b or its descendant. `shell.operator_context_idle_timeout_minutes` is changed only in the protected configuration file while Agent_b is stopped.
- `sandbox.enabled` is one install-global switch, defaults on, and replaces the removed per-folder map. Loading a legacy map sets it on when any entry was on. A ready sandbox is named deterministically for the session and sorted mount set, then receives the session scratch/retained root followed by only plan repositories selected by file operations in that run. An absent or signed-out `sbx` leaves ordinary shell routing unchanged and exposes the inert reason in state; `run_script` bash reports that reason because it has no host fallback.
- `POST /api/shell-credential {action:store,password}` writes a user-scoped DPAPI blob; `{action:test}` attempts a no-op service-account process; `{action:clear}` removes it. Responses and events expose status only, never the password.
- `GET /api/service-account` inspects the configured local account without elevation. `POST /api/service-account {action:create|reset,password,confirmation}` requires matching write-only values, stores the DPAPI credential, launches `setup-service-account.ps1` through Windows UAC, validates that credential through Windows, and enables the split. It never returns either password field. A canceled or unavailable UAC prompt makes no account change and restores the previous credential; a failure after the elevated helper starts is reported as potentially partial.
- `GET /api/hardening?connection_id=<id>` inspects complete-tree ACL and user-scoped outbound-firewall state, returns read-only `detected_local_subnets`, the applied LAN-switch state, and the most recent `operation {action,state,message,started_at,finished_at}` so progress and errors survive a browser refresh. `POST /api/hardening {action:apply|verify|remove,connection_id,allow_local_network?,local_subnets?}` orchestrates the policies; apply requires an enabled, stored non-administrator service identity and no active runs. LAN prefixes must be canonical detected RFC1918 IPv4 subnets and are persisted only after apply, status verification, and the real alternate-identity folder test succeed; direct `/api/config` writes to these keys are refused. Apply/remove use UAC. Apply and verify return `ok:false` with the component summaries when drift remains.
- `POST /api/host-window {action:minimize|maximize|close}` controls the native frameless window only while that host is present and returns 409 in browser mode. `POST /api/message {session_id,text,attachments?:[{path,bytes,sha256}]}` → 202 `{run_id,queued?,position?}`; attachment metadata must match an existing regular file directly beneath the session workspace's `attachments/` directory. An active same-session run returns 409 when queue depth is zero, and a full configured queue returns 409. `POST /api/stop {session_id?,all?}` holds queued messages until the operator's next message releases them in order. `POST /api/tools/{name} {session_id,enabled}` takes effect on the next request. `POST /api/approve {session_id,call_id,decision:once|session|deny}` resolves the existing card; an unknown call is 404. These mutation endpoints require the launch mutation token and are unavailable in replay mode.
- `GET /api/plan?session_id=<id>` enters Plan-page mode and returns the bound plan's `plan.md`, `NOTES.md`, id, and whether b is the planner fallback. A chat whose folder backs no plan gets 200 `{plan:"",notes:"",no_plan:true,reason}` (item 2fo), not a 409; other conflicts keep their 409. `GET /api/plan?plan_id=<id>` (item 2fc) returns `{plan_id,name,repo,plan,refusal,stats:{markers:{open,done,stuck,partial,dropped},changed_at?,last_go?,last_result?,running,related:[{id,name,repo}],chats:[{id,label}]}}` for a plan in the plans folder's own listing, 404 otherwise; it does not enter Plan-page mode. `POST /api/plans {repo}` registers the absolute folder through the same `EnsurePlan` route and refusals as every other registration and returns `{plan,created}`; an existing plan for the folder is returned, not duplicated. `POST /api/plans/build {plan_id,agent_id?,brief?:{purpose,done,do_not_touch}}` returns `{session_id,draft}`: the open d chat bound to the plan, or a new one on the agent's d connection (b when none). Each brief field is capped at 4,000 characters and is framed as scope data under explicit labels; all-empty produces the historical draft byte-for-byte. A chat with no message yet is sent `draft` as its first message (`sent:true,run_id`; since v0.70.1 the operator's Yes is the consent), and a chat already under way is only opened, with `draft` placed in its composer (`sent:false`); the harness composes no other message in the operator's name. `GET|POST /api/plan/go` and `GET /api/plan/worker` accept `plan_id` as well as `session_id`; Go is refused (409, and `enabled:false` with `refusal`) while the worker's model connection is serving as many runs as it may. `GET /favicon.ico` serves the application icon. While that mode is active, model-originated `write_file` and `edit_file` calls in a d-session refuse plan writes. `POST /api/plan/accept` accepts only a byte-identical proposal currently recorded in that session's tray: its kind is `add|reword|reorder|drop|agent_b_addition`, its clean relative path is `plan.md`, the direct `plan/items/<item_id>.md` child, or `AGENT_B.md` for the one-line addition kind, and `old_text` must match exactly once; add/drop/reorder semantics are checked against their labels. It opens the session-local acceptance gate for one existing D-path edit operation (including a transient D writer for b fallback), then replaces only the harness-bound source turn pair with `[settled → plan item N]`. `POST /api/plan/marker` flips only an exact current `[x]`/`[ ]` line. These mutation-token routes are browser actions; model output cannot accept itself.
- `GET /api/operator-files` reports operator-root attachment count/bytes and instruction candidates. Its mutation-token-protected `POST` actions are `empty_attachments` (requires `confirm:true`) and `adopt_instructions` (creates `AGENT_B.md` only when absent; legacy cleanup additionally requires `cleanup:true,confirm_cleanup:true`). `GET /api/operator-attachments` lists top-level regular files and `GET /api/operator-attachments?path=<listed-basename>` returns one; neither exposes a host path. `POST /api/ui-errors` accepts a bounded browser error with optional session ID plus `repeat_count` and `capped`, then emits it to the harness log. A relayed UI error updates client evidence state without notifying rendering subscribers. The mailbox toggle and retention value use `/api/config`; changing the mailbox toggle requires the verified local-operator process check.

## Replay and keyboard

`go run ./cmd/harness -config harness.json -replay <a.jsonl>[,<b.jsonl>…]` projects every JSONL record with the same pure projector used live, orders the resulting patches by recorded timestamp, and starts without probing, model clients, tools, or writers. Replay status is a transport/UI overlay; it does not mutate the historical projection. All API GETs remain available; session/config/connection mutations, messages, stops, approvals, and tool toggles return 409 `{"error":"replay mode"}`.

The main page uses `Ctrl+1` through `Ctrl+9` to switch sessions and Escape to close settings. Chat uses `/` to focus the composer, `Ctrl+.` to stop, Enter to send, Shift+Enter for a newline, and Escape to clear the draft. Every interactive control is in normal Tab order with a visible Trace focus ring.

## Behavior notes

- `sandbox.workspaces` maps absolute workspace paths to explicit boolean declarations. For a declared workspace with an available, signed-in `sbx`, `shell` ensures a deterministic `agentb-<hash>` shell sandbox exists with only that workspace mounted, then uses `sbx exec`; `run_script` adds `bash` and supplies its source on standard input without a TTY. Both paths retain the ordinary approval policy and use the existing operator-identity decision. If capability discovery is not ready, the declaration is inert and returns the one-line `sandbox.reason`; undeclared workspaces retain host behavior and never create a sandbox. The registry is thirteen tools; `delegate` follows the twelve-tool 2ho order.

- A write from one session blocks another session's `write_file` or `edit_file` until that session re-reads the shared path; the refusal identifies the other session and publishes `workspace.conflict`. With the service split enabled, file tools impersonate that account and Windows ACLs decide reach. With it disabled, file tools use the non-elevated operator's OS reach; plan ownership and repository-policy immutability remain enforced in both postures.
- Tool order is `read_file`, `list_dir`, `write_file`, `edit_file`, `search`, `shell`, `remember`, `recall`, `fetch_url`, `web_search`, `run_script`, `call_service`, `delegate`. `read_file`, `fetch_url`, and `call_service` use one-based response byte offsets and limits; their model-visible descriptions tell callers to pass a returned `next_offset` as `offset`. If a successful window is too large to leave room for another model turn, the dispatcher replaces it with a failed compact result that names the same offset and a smaller retry limit; it never appends an individually impossible result. After such a refused `read_file` window the end-of-turn elide runs even on a cold prefill; a window refused again on the next turn of the same run ends the read instead (item 2fv; refusals of parallel calls in one turn, and a `windows` batch, do not count): the result is a successful note, `note: the read was cut short: …`, metadata `read_cut_short:true`, and the run's later requests carry `tool_choice:"none"`, so the model answers from what it read and says the read was cut short. `read_file` additionally guarantees UTF-8 boundaries, reports `start_line`, `start_mid_line`, and `end_mid_line`, and prefixes each returned source line with its line number; its byte counts cover source bytes, not the added presentation prefixes. The memory tools are intentionally not impersonated: they can reach only the application-derived note file for the active workspace and accept no path input. `fetch_url` and `web_search` remain anonymous and their result tokens are charged to `fetched`; `call_service` is identity-bearing and its results use ordinary `results` accounting.
- `shell` accepts inline commands. Execution-policy bypass is rejected before process creation. Commands that create `.ps1`, `.psm1`, `.cmd`, `.bat`, `.vbs`, `.wsf`, or `.sh` artifacts are rejected, and script execution is rejected when the target was recorded as written by an agent file tool during the current run. Existing script files without that run provenance remain executable.
- Messages posted during a run wait per session and start in order after it ends, including after `user_stop`. Positive `run.queue_depth` values cap each session's pending queue; depth `0` means unbounded. A full positive-depth queue returns 409.
- `run.cycle_window: 0` disables repeated-call cycle detection. `run.max_consecutive_tool_errors: 0` disables the consecutive tool-error stop. `run.max_wall_clock_seconds` and `run.max_tool_calls` are independent enforcing backstops; their defaults are the explicitly guessed 21,600 seconds and 1,000 calls. The existing 10,000-turn default remains unchanged.
- Live SSE subscribers have a 128-patch queue. A full queue closes the stream so EventSource reconnects at a fresh atomic snapshot; patches are never silently dropped. JSONL remains the sole durable session authority. A failed append does not advance the projector, marks it stale, and closes subscribers for resynchronization. Completed raw model deltas are diagnostic records and are omitted from the completed-turn projection.

## Configuration

- `connections[].capabilities.vision` is `reads images`, `accepts images but does not read them`, or `rejected`; automatic native image routing requires the first value. `sandbox.workspaces` is an object whose absolute-path keys explicitly select Docker Sandbox execution. Both keys are present in `harness.example.json` and are additive under the current schema.

Every parsed key appears in `harness.example.json`; connector entries are `services.<name>.{kind,base_url,auth,allowed_methods,timeout_s,max_body_kb,require_confirmation}`. `kind` is `http` or `mcp`; MCP entries use POST. Auth is `none`, `static_bearer:<env>`, or `exec:<argv>`; connector proposals require an exec helper to end in `token` or `headers`. Services default to an empty object. Other configuration remains additive under schema 10; omitted policy lists receive defaults while explicit empty lists clear them. `operator_context` always saves false and starts off, mailbox approval answers default off, and delivery defaults to `both` at `%USERPROFILE%\Agent_b`.

`GET` or `HEAD /api/files/<workspace-relative-path>?session=<id>` serves one regular file from the named session workspace with `Content-Disposition: attachment`. It calls the same `tools.Resolve` jail entry point as built-in file tools; jail failures, absent files, and directories are all 404, and there is no directory listing. `POST /api/open-folder` accepts `{session_id,path,scope:workspace|exchange}` under the launch mutation guard, jail-resolves the selection against that root, and asks the operator desktop to reveal the file. `POST /api/open-file` takes the same body and asks the operator desktop to open the file with its default application (item 2ep: an `.xlsx` opens in Excel); it opens only document types (`.xlsx .docx .pptx .pdf .txt .md .json .log .png .jpg .jpeg .gif .webp`) and answers 415 for anything whose default action runs it. The Chat delivery chip opens a document from its name and its folder from a folder glyph ("folder" on hover).

`POST /api/attachments` accepts mutation-token-protected multipart fields `session_id` and one `file`, jail-resolves `attachments/<sanitized-basename>`, and returns `{path,bytes,sha256,kind,reused?,tier?,sidecar?,note?}`. An `.xlsx` or `.docx` sidecar is structured Markdown (item 2ep): a workbook is one table per sheet under a `## <sheet name>` heading, rows and columns intact, shared strings resolved, formulas as their cached values, merged cells expanded, the header row repeated every 100 body rows; a document is headings, paragraphs, list items and tables in reading order. Other Office types keep the plain text sidecar. `write_file` with a path ending `.xlsx` turns Markdown tables (one per `## sheet name`) into a workbook of inline strings and numbers, never formulas, and refuses content with no table; every other extension is written as the bytes given. It streams through an attachment-specific size bound, refuses ZIP and Windows device filenames, never overwrites, and reuses identical content. `GET /api/exchange-files` enumerates top-level regular files in the configured exchange folder as attachment metadata; `GET /api/exchange-files?path=<listed-basename>` returns one listed file so the Chat client can feed the same multipart ingest path. Neither endpoint exposes a host path to the browser.

Attachment request rendering is connection-dependent. Text/code, SVG/XML, and strictly valid UTF-8 text with an otherwise unknown extension stay normal `read_file` targets. DOCX/XLSX/PPTX receives a bounded standard-library XML text sidecar. With `attachment_handling:auto`, PDF becomes an in-memory `file` content part when `document_input` was probed, otherwise it uses `extract_url` when configured and stores an explicitly untrusted sidecar, otherwise (item 2fj) its text layer is read locally into an untrusted sidecar (tier `extracted`), and a PDF with no text layer is rendered and read by the inbox OCR page by page, at most 20 pages (tier `ocr`); only when neither reads it is it labeled unreadable binary with the reason; PNG/JPEG/GIF/WebP becomes an in-memory `image_url` content part when `image_input` was probed and otherwise receives a local Windows OCR sidecar. `native` sends those document/image kinds regardless of the probe, while `extract` never sends their binary bytes. Each native part is immediately preceded by its own harness-generated text part declaring that payload untrusted evidence, never instructions. Native data-URL characters are conservatively upper-bounded one-for-one as context tokens and allocated cumulatively across retained attachment history against `n_ctx - reserve_output`; a part that does not fit is omitted with an explicit attachment outcome visible to both Chat and the model. OCR sidecars are labeled untrusted and state that layout was not preserved; a no-text result is reported explicitly and creates no sidecar. Other binary types are stored but labeled unreadable. Native bytes are deliberately omitted from durable `model.request.body` diagnostics.

At run end, the file set carried by successful `write_file`/`edit_file` results is copied to the exchange folder in `folder` or `both` mode. Basename collisions receive ` (2)`, ` (3)`, and so on; an existing file with the same SHA-256 is reused without overwrite. Exactly one durable `files.delivered` event is emitted per run with the mode, exchange root, and per-file source path, delivered path, SHA-256, and `copied|identical|failed` status. Bytes remain outside JSONL.

Application and data roots are independent runtime inputs. The normal installed layout is `%LocalAppData%\Programs\Agent_b`, `%LocalAppData%\Agent_b`, and `%LocalAppData%\Agent_b-workspace`; explicit all-users installation uses Program Files/ProgramData. New chat roots live under the data root's `scratch` child. An explicit `-config` wins, followed by `AGENTB_CONFIG`, an existing `%LocalAppData%\Agent_b\harness.json`, and finally CWD-relative `harness.json` for development.

Connections with `tool_calls=false`, `overflow_behavior=truncate`, `streaming=false`, or unknown context are not runnable. `context.accounting=exact` later refuses connections without `/tokenize` using `exact accounting requested but this server has no /tokenize`; `auto` chooses exact where possible and calibrated estimation elsewhere; `estimated` forces estimation.

## Phone authentication and push

The listener remains on `127.0.0.1`; a tailnet-only `tailscale serve` HTTPS proxy may expose it without changing the bind. `POST /api/phone/enrolment` is desktop-authenticated and returns a six-digit, five-minute, single-use in-memory code. Public `POST /api/phone/enrolment/redeem` is rate-limited and exchanges that code for one random per-device bearer credential; only its SHA-256 hash is retained. A valid device bearer has full desktop API authority by design, including shared `/api/state`, `/api/events`, and `/api/message`; per-device or all-device revocation invalidates the next request and cancels an active stream, while desktop cookies remain separate. The root-scope phone service worker persists and attaches the bearer only for requests initiated by `/phone` clients, so it does not authenticate desktop pages. Web Push uses a DPAPI-protected P-256 VAPID private key and profile-local subscriptions, removes HTTP 404/410 endpoints, and sends only the existing human notice, chat name, and chat deep link for `ApprovalRequired`, `RunStopped`, and `ItemStuck`.

## Implementation status

Every `[prompt N]` tag and every record/API shape above now has an implementation. Session state has one pure versioned projector shared by live snapshots, live patches, and replay; browser-local expansion, scrolling, tab selection, and Settings drafts remain view state.
